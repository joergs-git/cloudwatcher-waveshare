# CloudWatcher Waveshare Display - ESP-IDF Project
# v0.1.0

cmake_minimum_required(VERSION 3.16)

# Pull in ESP-IDF build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(cloudwatcher-waveshare)
